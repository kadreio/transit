{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Server",
            "type": "node",
            "request": "launch",
            "args": ["${workspaceRoot}/index.ts"],
            "runtimeArgs": ["-r", "ts-node/register"],
            "protocol": "inspector"
        }
    ]
}
